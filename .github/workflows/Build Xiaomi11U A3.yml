name: Build Kernel (5.4)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  TZ: Asia/Shanghai
  DEVICE_NAME: "xiaomi_11U"
  DEVICE_CODENAME: "star"
  KERNEL_ARCH: "arm64"
  # 以下变量可保留，但在脚本中我们直接导出 LLVM 等，不再依赖这些变量
  CUSTOM_CMDS: "CLANG_TRIPLE=aarch64-linux-gnu-"
  EXTRA_CMDS: "LD=ld.lld LLVM=1 LLVM_IAS=1"

  KERNEL_REPO: "zttlove/android_kernel_xiaomi_sm8350_miyume"
  KERNEL_BRANCH: "main"                     # 如果是 5.4 分支，改为 "5.4"

  CLANG_SOURCE: "https://github.com/llvm/llvm-project/releases/download/llvmorg-18.1.8/clang+llvm-18.1.8-x86_64-linux-gnu-ubuntu-18.04.tar.xz"
  GCC_GNU: false
  # GCC 相关源保留但不会被下载（因为 GCC_GNU=false）
  DEFCONFIG_NAME: "star_defconfig"

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Kernel Source
        uses: actions/checkout@v4
        with:
          repository: ${{ env.KERNEL_REPO }}
          ref: ${{ env.KERNEL_BRANCH }}
          path: kernel

      - name: Install Dependencies
        run: |
          sudo apt update
          sudo apt install -y bc bison flex libelf-dev libssl-dev \
            make python3 device-tree-compiler git curl xz-utils

      - name: Download Clang
        run: |
          curl -L -o clang.tar.xz "${{ env.CLANG_SOURCE }}"
          mkdir -p clang
          tar -xf clang.tar.xz -C clang --strip-components=1
          echo "$(pwd)/clang/bin" >> $GITHUB_PATH

      # 可选的 GCC 下载步骤被跳过（因为 GCC_GNU=false）
      # 若需要备用 GCC，可取消注释并调整条件

      - name: Build Kernel
        working-directory: kernel
        run: |
          export ARCH=${{ env.KERNEL_ARCH }}
          export LLVM=1
          export LLVM_IAS=1
          export LD=ld.lld
          export CLANG_TRIPLE=aarch64-linux-gnu-
          make ${{ env.DEFCONFIG_NAME }}
          make -j$(nproc)

      - name: Upload Kernel Image
        uses: actions/upload-artifact@v4
        with:
          name: kernel-image
          path: |
            kernel/arch/arm64/boot/Image
            kernel/arch/arm64/boot/Image.gz
            kernel/arch/arm64/boot/Image.gz-dtb
            kernel/arch/arm64/boot/dts/**/*.dtb
