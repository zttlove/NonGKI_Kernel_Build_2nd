name: 'Patch SuSFS for Kernel.'
runs:
  using: 'composite'
  steps:
    - name: SKIP ALL PATCHES
      shell: bash
      run: |
        echo "SUSFS_SKIP=true" >> $GITHUB_ENV
        echo "SKIP_PATCH=true" >> $GITHUB_ENV
